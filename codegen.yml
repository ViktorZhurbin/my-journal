schema: 'backend/src/schema.ts'
generates:
    'backend/src/types.d.ts':
        plugins:
            - typescript
            - typescript-resolvers
        config:
            federation: true
            useIndexSignature: true
watch: true
